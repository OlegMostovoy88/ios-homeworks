# Xcode
#
# участники gitignore: не забудьте обновить Global/Xcode.gitignore, Objective-C.gitignore и Swift.gitignore

## Пользовательские настройки
xcuserdata/

## совместимость с Xcode 8 и более ранними версиями (игнорирование не требуется, начиная с Xcode 9)
*.xcscmblueprint
*.xccheckout

## совместимость с Xcode 3 и более ранними версиями (игнорирование не требуется, начиная с Xcode 4)
построить/
Производные данные/
*.отодвигается в сторону
*.pbxuser
! default.pbxuser
*.mode1v3
! default.mode1v3
*.mode2v3
! default.mode2v3
*.perspectivev3
! default.perspectivev3

## Obj-C/Swift specific
*.hmap

## Упаковка приложения
*.ipa
*.dSYM.zip
*.dSYM

## Игровые площадки
timeline.xctimeline
playground.xcworkspace

# Быстрый менеджер пакетов
#
# Добавьте эту строку, если вы хотите избежать получения исходного кода из зависимостей Swift Package Manager.
# Пакеты/
# Пакет.pins
# Пакет.решено
# *.xcodeproj
#
# Xcode автоматически генерирует этот каталог с файлом .xcworkspacedata и xcuserdata
# следовательно, это не нужно, если вы не добавили файл конфигурации пакета в свой проект
# .swiftpm

.build/

# Какао-поды
#
# Мы рекомендуем не добавлять каталог Pods в ваш .gitignore. Однако
# вы должны судить сами, плюсы и минусы упоминаются в:
# https://guides.cocoapods.org/using/using-cocoapods.html#should-i-check-the-pods-directory-into-source-control
#
# капсулы/
#
# Добавьте эту строку, если вы хотите избежать проверки исходного кода из рабочей области Xcode
# *.xcworkspace

# Карфаген
#
# Добавьте эту строку, если вы хотите избежать проверки исходного кода из зависимостей Карфагена.
# Карфаген/Кассные кассы

Карфаген/Строительство/

# Управление зависимостями Accio
Зависимости/
.accio/

# фастлейн
#
# Рекомендуется не хранить скриншоты в репозитории git.
# Вместо этого используйте fastlane для повторной создания скриншотов всякий раз, когда они необходимы.
# Для получения дополнительной информации о рекомендуемой настройке посетите:
# https://docs.fastlane.tools/best-practices/source-control/#source-control

fastlane/report.xml
fastlane/Preview.html
fastlane/скриншоты/**/*.png
fastlane/test_output

# Ввод кода
#
# После нового кода Инструменты для инъекций создается папка /iOSInjectionProject
# https://github.com/johnno1962/injectionforxcode

Проект iOSInjectionProject/
wq
